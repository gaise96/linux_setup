# install operator font
sudo apt install fonttools &&
sudo apt install fonttools &&
git clone git@github.com:kiliman/operator-mono-lig.git &&
cd operator-mono-lig &&
sudo chnod +x build.sh &&
./build.sh
